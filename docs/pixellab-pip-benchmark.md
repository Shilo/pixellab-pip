# PixelLab Pip Skill Benchmark

This benchmark measures what the PixelLab Pip skill costs an agent and how well it routes, compared with the other ways to give an agent PixelLab knowledge:

- **`current`** — the Pip skill (this repo's working tree): `SKILL.md` injected, `references/*.md` read on demand.
- **`pre-kiss-yagni-refactor`** — the Pip skill before its size refactor, for the efficiency-over-time view.
- **`mcp-docs`** — no skill, but PixelLab's official [`api.pixellab.ai/mcp/docs`](https://api.pixellab.ai/mcp/docs) injected into the session (the [pixellab.ai/mcp](https://www.pixellab.ai/mcp) "include this link in your prompts" tip).
- **`vanilla`** — no skill and no docs: the agent's own knowledge only.

It measures the **agent session only** (context tokens, output, cost, routing correctness) — never PixelLab credits. Everything here is produced by [`dev-tools/skill_benchmark.py`](../dev-tools/skill_benchmark.py) and is reproducible; see [Reproduce](#reproduce). Routing correctness is scored by deterministic regexes (no LLM judge), so the same response always scores the same.

## Results

The tables below are generated by the benchmark itself. **Context size** is deterministic and reproducible (chars/4 estimates). Because the skill loads references **on demand**, its real per-request cost is the per-scenario context row — only the reference a task needs is loaded, never the whole corpus at once. **Routing correctness** (`checks_rate`) is the fraction of a scenario's deterministic checks the response passed (1.0 = fully correct); those numbers come from live agent runs and are a dated snapshot that varies between runs.

<!-- BENCHMARK:GENERATED:START -->
_Auto-generated by `dev-tools/skill_benchmark.py --report` on 2026-07-05 — variants: current, pre-kiss-yagni-refactor, mcp-docs, vanilla; agents: claude; 2 rep(s). Edits inside this block are overwritten on the next run._

### Context size (estimated tokens, chars/4)

| Scope | `current` | `pre-kiss-yagni-refactor` | `mcp-docs` | `vanilla` |
|---|---|---|---|---|
| Injected context (SKILL.md / MCP docs / none) | 7,227 | 10,756 | 7,745 | 0 |
| route-hex-tiles | 7,227 | 10,756 | 7,745 | 0 |
| route-character | 7,227 | 10,756 | 7,745 | 0 |
| plan-item-icon-sheet | 9,192 | 16,091 | 7,745 | 0 |
| cheap-animation | 8,473 | 13,163 | 7,745 | 0 |
| report-format | 8,506 | 13,474 | 7,745 | 0 |
| setup-codex | 12,043 | 19,566 | 7,745 | 0 |
| refuse-internal-endpoint | 7,227 | 10,756 | 7,745 | 0 |
| skeleton-pipeline | 12,343 | 18,396 | 7,745 | 0 |
| live-balance | 8,506 | 13,474 | 7,745 | 0 |
| live-cheap-image | 9,752 | 15,881 | 7,745 | 0 |

### Routing correctness and session input (medians)

| Scenario | Agent | Metric | `current` | `pre-kiss-yagni-refactor` | `mcp-docs` | `vanilla` |
|---|---|---|---|---|---|---|
| cheap-animation | claude | routing correct | 1 | 1 | 0.5 | 0.5 |
| cheap-animation | claude | input tokens | 39,117 | 53,920 | 15,294 | 4,912 |
| refuse-internal-endpoint | claude | routing correct | 1 | 1 | 1 | 0.25 |
| refuse-internal-endpoint | claude | input tokens | 13,874 | 43,923 | 15,278 | 3,042 |
| route-character | claude | routing correct | 1 | 1 | 0.667 | 0 |
| route-character | claude | input tokens | 13,894 | 18,536 | 15,299 | 3,059 |
| route-hex-tiles | claude | routing correct | 1 | 1 | 1 | 0 |
| route-hex-tiles | claude | input tokens | 13,882 | 18,524 | 15,289 | 3,046 |
<!-- BENCHMARK:GENERATED:END -->

## Reading the results

- **`vanilla` is cheapest but least reliable.** With no PixelLab knowledge it tends to pick the wrong tool and to miss safety refusals — low correctness for near-zero context.
- **`mcp-docs` routes well on tool selection** because it carries the full MCP inventory in context, but it can miss task-specific *cost* and *ordering* knowledge that the general docs do not specialize in.
- **`current` (the skill)** routes correctly across the benchmark scenarios. On simple routing its session context is comparable to — often slightly leaner than — injecting the official docs, because it only injects `SKILL.md` up front. On a harder question it deliberately reads one reference (e.g. `cost-routing.md`), spending a few more tokens to return the correct documented answer — the reference load is what buys the right answer.
- **The refactor** cut the base `SKILL.md` context roughly a third versus `pre-kiss-yagni-refactor`, visible in the context table.

The two context strategies (skill vs official docs) are complementary, not rivals — the skill itself treats the official docs as the authoritative MCP inventory; see [Official MCP Docs vs Pip Skill](tools/pixellab-mcp-docs-vs-pip-skill.md).

## Reproduce

Free, deterministic, no CLI calls or secret — this reproduces the **context size** table exactly:

```powershell
python dev-tools/skill_benchmark.py --static --variants pre-kiss-yagni-refactor,mcp-docs,vanilla
```

Live routing (needs an agent CLI; numbers vary run to run). Add `--report` to rewrite the generated block in this page from the run:

```powershell
python dev-tools/skill_benchmark.py --agents claude --variants pre-kiss-yagni-refactor,mcp-docs,vanilla `
  --scenarios route-hex-tiles,route-character,cheap-animation,refuse-internal-endpoint --reps 2 `
  --report docs/pixellab-pip-benchmark.md
```

On Windows, [`dev-tools/run-skill-benchmark.ps1`](../dev-tools/run-skill-benchmark.ps1) wraps the common runs. Its **`full`** preset runs every agent and every variant, live + paid, and refreshes this report automatically. Full methodology, arms, and flags are in [Developer → Skill Token Benchmark](developer.md#skill-token-benchmark).

## Method and caveats

- **Agent session only.** Tokens/cost are the agent's, not PixelLab credits.
- **Static tokens are chars/4 estimates**, not a tokenizer count; treat them as relative, not absolute.
- **The routing snapshot is nondeterministic.** Agent runs vary; the context table is the reproducible part. Rerun with more agents/reps (or the `full` preset) for your own numbers. The generation note above records when it was last measured and with which agents/reps.
- **Fairness controls:** each variant runs in an isolated workspace outside the repo (so no stray `CLAUDE.md`/`AGENTS.md` leaks in), variants interleave within each rep, medians are reported, and routing is scored by fixed regexes rather than a model.
- **`mcp-docs` content is fetched live** from `api.pixellab.ai/mcp/docs` at run time and saved alongside the results for audit.
